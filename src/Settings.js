import React from 'react'

const Settings = ({name}) => {
  return (
    <div className='main'>
        <section className="settings">
            {name}
        </section>
    </div>
  )
}

export default Settings