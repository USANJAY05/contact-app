import React from 'react'

const Groups = ({name}) => {
  return (
    <div className='main'>
        <section className='groups'>
            {name}
        </section>
    </div>
  )
}

export default Groups